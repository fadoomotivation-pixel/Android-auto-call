<resources>
    <style name="Theme.SalesAutoCall" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Branded splash shown while the app process starts -->
    <style name="Theme.SalesAutoCall.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.SalesAutoCall</item>
    </style>
</resources>
